<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naver.dao.CommentsMapper">
    <insert id = "insertComment">
        insert into contents (user_id,attribute, value)
        values (#{user_id},#{attribute},#{value});
    </insert>
    <update id="plusViews">
        update user
        set num_of_visit= num_of_visit +1
        where id= #{id}
    </update>
    <delete id="deleteComment">
        DELETE FROM contents
        where user_id= #{user_id} and attribute = #{attribute} and value= #{value}
    </delete>
    <select id="getUser" resultType="User">
        select id, name, num_of_visit, sub_data
        from user
        where id=#{id}
    </select>

</mapper>