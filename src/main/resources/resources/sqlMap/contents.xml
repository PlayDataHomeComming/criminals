<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naver.dao.ContentsMapper">

    <insert id="addContents" >
        insert into contents(user_id, attribute, value)
        values (#{user_id}, #{attribute}, #{value})
    </insert>

    <select id="findAll">
        select id, user_id, attribute, value
        from contents where user_id = #{user_id}
    </select>

</mapper>