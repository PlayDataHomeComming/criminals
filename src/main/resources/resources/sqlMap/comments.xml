<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naver.dao.CommentsMapper">

    <select id="getComments" resultType="Comments">
        select * from comments
        where user_id=#{userId}
    </select>

    <insert id = "insertComment">
        insert into comments (user_id,nick_name, content, password)
        values (#{userId},#{nickName},#{comment},#{passWord});
    </insert>

    <delete id="deleteComment">
        DELETE FROM contents
        where user_id= #{user_id} and attribute = #{attribute} and value= #{value}
    </delete>

    <update id="updateImgUrl">
        update user
        set image_url=#{imgUrl}
        where id=#{userId}

    <update id="updateComment">
        update comments
        set content=#{comment}
        where id=#{commentId} and password=#{passWord} and user_id=#{userId}

    </update>

</mapper>